    /** \brief Realiza la suma entre dos operadores
     *
     * \param a float Recibe el primer operador
     * \param b float Recibe el segundo operador
     * \return float Devuelve el resultado
     *
     */
    float suma(float a, float b);

    /** \brief Realiza la resta entre dos operadores
     *
     * \param a float Recibe el primer operador
     * \param b float Recibe el segundo operador
     * \return float Devuelve el resultado
     *
     */
    float resta(float a, float b);

    /** \brief Realiza la multiplicacion entre dos operadores
     *
     * \param a float Recibe el primer operador
     * \param b float Recibe el segundo operador
     * \return float Devuelve el resultado
     *
     */
    float multiplicacion(float a, float b);

    /** \brief Realiza la division entre dos operadores
     *
     * \param a float Recibe el primer operador
     * \param b float Recibe el segundo operador
     * \return float Devuelve el resultado
     *
     */
    float division(float a, float b);

    /** \brief Realiza la factorizacion del primer operador
     *
     * \param a int Recibe el primer operador
     * \return int Devuelve el resultado
     *
     */
    int factorialUno(int a);
    /** \brief Realiza la factorizacion del primer operador
    *
    * \param a int Recibe el primer operador
    * \return int Devuelve el resultado
    *
    */
    int factorialDos(int b);
    /** \brief Realiza el menu de opciones
     *
     * \param -
     * \param -
     * \return -
     *
     */
     void opciones(void);
